security:
  jwt:
    secret-key: ${JWT_KEY}
    expiration-time: ${JWT_DURATION}

spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

  flyway:
    enabled: true